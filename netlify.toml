# Noorvi Fashion — Netlify configuration
# Client-demo branding deploy: shop name is editable from Admin → Store Information.
[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: send every path to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
