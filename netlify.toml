# Noorvi Fashion — Netlify configuration
[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: send every path to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Production refresh: editable Shop name is enabled in Admin → Store Information.
# Verified live preview trigger for editable Shop name.
